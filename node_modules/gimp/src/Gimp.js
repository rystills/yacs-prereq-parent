var Session = require('./Session');

module.exports = class Gimp {

	constructor(binSrc = '/usr/bin/env gimp') {
		this.binSrc = binSrc;
	}

	session() {
		return new Session(this.binSrc);
	}
}

module.exports.Session = Session;